To run  from Python interpreter

from sumProduct import *
from colormle import *
from colorem import *
from sumProduct import *

To run 'colormle', set A, samples, then colormle(A, samples)

To run 'colorem', set A, L, samples, then colorem(A, L, samples)

Alternatively, you can use myDemo:
paste in your A, samples, L, and uncomment the function you want to run.
